name: Build CI - Windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
  
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build Artifact
      run: call  3_Implementation
 
